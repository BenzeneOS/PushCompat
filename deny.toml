# cargo-deny config. Every category is a CI gate.
# Run locally with:  cargo deny check

[graph]
all-features        = true
no-default-features = false

[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = [ "https://github.com/rustsec/advisory-db" ]
# Needs rand's `log` feature, which nothing here enables. The fix is
# rand 0.9, and Android.bp pins `librand-0.8` for the Soong build.
ignore  = [ "RUSTSEC-2026-0097" ]
version = 2
yanked  = "warn"

[licenses]
allow = [
  "MIT",
  "Apache-2.0",
  "Apache-2.0 WITH LLVM-exception",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "ISC",
  "Unicode-3.0",
  "Zlib",
  "BSL-1.0",
  "CDLA-Permissive-2.0",
  "EUPL-1.2",
]
confidence-threshold = 0.9
version = 2

[bans]
deny              = [ { name = "native-tls" }, { name = "openssl" }, { name = "openssl-sys" } ]
highlight         = "all"
multiple-versions = "warn"
wildcards         = "deny"

[sources]
allow-registry   = [ "https://github.com/rust-lang/crates.io-index" ]
unknown-git      = "deny"
unknown-registry = "deny"
